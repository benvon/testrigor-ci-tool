version: "2"

run:
  timeout: 5m
  go: '1.25'
  allow-parallel-runners: true

linters:
  default: standard

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - dupl
          - gosec
          - funlen
        